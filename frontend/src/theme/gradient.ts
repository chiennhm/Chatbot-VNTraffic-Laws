import { colors } from "./color";

export const gradients = {
    brand: `linear-gradient(135deg, ${colors.brand.c3}, ${colors.brand.c1}, ${colors.brand.c2})`,
    glass: `linear-gradient(180deg, rgba(255,255,255,0.08), rgba(255,255,255,0.03))`,
};
